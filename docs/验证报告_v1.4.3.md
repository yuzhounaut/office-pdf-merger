# v1.4.3 验证结果

## 本版修复与验证项目

1. **带点文件名保留验证**：
   - 测试文件名：`18.1-杨培茵（CV、GCP、执业证书）`
   - 验证结果：生成的目标 PDF 名称完整为 `18.1-杨培茵（CV、GCP、执业证书）.pdf`，未被截断为 `18`。
   - 包含多点号（如 `v1.2.3`、`2026.09.20`）均完整正确保留。

2. **清单与日志生成目录验证**：
   - 验证结果：输出目录中仅包含生成的最终 PDF，不再包含 `.清单.csv` 和 `.日志.txt`。
   - 检查程序目录 `OfficePdfData`：清单文件 `18.1-杨培茵（CV、GCP、执业证书）.清单.csv` 及日志文件 `18.1-杨培茵（CV、GCP、执业证书）.日志.txt` 已正确写入并保存。

3. **重名避让逻辑验证**：
   - 验证带点文件名与分组目录名称在存在同名文件/文件夹时的序列编号生成情况（如 `(2)`），扩展名与主干名称无异常拆分。

4. **基础架构与图标保持**：
   - 继承 v1.4.2 统一矢量及全尺寸位图图标。
   - 随附原始 qpdf 二进制依赖，保证转换流程完全本地离线运行。
